你是一名 Shopify 主题开发专家，请帮我开发一个 Shopify 自定义 Section，要求如下：

---

【设计图】
（上传截图）

---

【基础要求】
1. 创建两个文件：
   - sections/[文件名].liquid
   - assets/[文件名].css
2. CSS 通过 asset_url 引入，不写在 liquid 里
3. 所有文字、图片、颜色、链接必须在后台可编辑
4. 使用 blocks 管理可重复内容（卡片、列表项、Tab等）
5. 代码符合 Shopify theme-check 规范，无报错
6. 所有 Section 必须包含完整 {% schema %} 块
7. 图片使用 image_url filter 处理响应式
8. 不使用 Vue/React，只用原生 Liquid + JS
9. 遵循 Shopify 主题开发最佳实践
10. img标签要给宽和高属性值

---

【后台可编辑项】
Section Settings（全局）：
- 区块标题
- 背景颜色
- 内容最大宽度
- 上下内边距

Block Settings（每个重复单元）：
- 图片上传
- 标题文字
- 副标题/描述
- 按钮文字 + 链接
- 其他根据设计图判断

---

【响应式要求】
- 桌面（>1024px）：按设计图还原
- 平板（768px-1024px）：等比缩小，保持结构
- 手机（<768px）：单列或双列，内容可读

---

【视觉还原要求】
请深度分析设计图，识别：
1. 整体布局结构（几列、几行、嵌套关系）
2. 卡片结构（图片在上还是在下、文字层级）
3. 颜色（背景色、文字色、强调色）
4. 特殊效果（玻璃拟态、渐变遮罩、hover动效）
5. 间距和圆角
6. 字号和字重层级

---

【品牌规范】
- 主色：#0067FF
- 字体：Helvetica Neue, Arial, sans-serif
- 圆角：12px
- 按钮圆角：50px
- 最大宽度：1200px

---

【输出格式】
请按以下顺序输出：
1. 文件名和路径
2. liquid 完整代码
3. css 完整代码
4. 后台配置说明（如何添加分区、填写内容）